<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose owns all theming; this is just the pre-Compose window shell -->
    <style name="Theme.ZeroFomo" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
